# All VITE_ vars are embedded in the JS bundle at build time — do NOT put secrets here.

# WhatsApp business number (digits only, with country code)
VITE_WHATSAPP_NUMBER=

# Backend API base URL
VITE_API_URL=
